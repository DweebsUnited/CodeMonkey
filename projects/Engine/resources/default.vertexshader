#version 410

layout( location = 0 ) in vec3 inPos;
layout( location = 1 ) in vec3 inCol;

uniform mat4 mvpMat;

out vec3 outCol;

void main( void ) {
    outCol = inCol;
    gl_Position = mvpMat * vec4( inPos, 1.0 );
}
